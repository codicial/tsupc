import { defineConfig } from "tsdown";

export default defineConfig({
  entry: ["src/index.ts"],
  clean: true,
  dts: {
    sourcemap: true,
  },
  format: ["esm", "cjs"],
  sourcemap: true,
  target: "es2020",
});
