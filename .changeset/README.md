# Changesets

This repository uses Changesets to manage package versions and changelogs.

Create a new changeset with `pnpm changeset`, then commit the generated markdown file alongside your package changes.
