# `@tsupc/predicate`

A collection of predicates for TypeScript and JavaScript.

## Install

```sh
npm install @tsupc/predicate
```

## Usage

```ts
import { isTrue } from "@tsupc/predicate";

// TODO: Demonstrate some usage here.
isTrue(true);
```

CommonJS is supported as well:

```js
const { isTrue } = require("@tsupc/predicate");
```

## API

TODO: Detail the packages API. Example:

```md
### `isTrue(bool)`

Returns `true` or `false`.
```

## License

MIT
